# cnn-pipeline-rtl
ECE 464 Project: Hardware implementation of a convolutional neural network (CNN) pipeline that applies a configurable 4×4 kernel across a 1024×1024 image stored in DRAM, using SRAM as a scratchpad to maximize throughput.
